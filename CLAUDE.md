# beautypark-librarylab

뷰티파크의원 범어점의 마케팅 자동화 시스템. `dashboard/`는 월별 이벤트 요청서(엑셀)를 업로드해
포스터·플친/인스타 카피를 만드는 React(Vite) 앱이다. Vercel로 배포된다.

```bash
cd dashboard && npm install && npm run build   # 타입체크 포함 빌드
```

## 이벤트 요청서 엑셀 구조 (매월 시트 1장)

- 시트명 `YYYY.M` (예: `2026.9`) · A1 = `N월 디자인 작업 요청서`
- 5행부터 데이터. **A=타이틀(그룹, 세로 병합)** · **B=시술명** · **C=정상가(부가세 전)** ·
  **D=이벤트가(부가세 전)** · **E=이벤트가 부가세 포함(원내확인용)** · **I=비고**
- `월 초 강조 : … / 월 중순 강조 : …` 행이 그룹들 아래에 병합 블록으로 존재
- 장기(런칭·시즌) 이벤트는 타이틀에 `(이벤트 기간 : 26.08.01 ~ 26.10.31)` 형식으로 기간 명시
- 파서(`dashboard/src/lib/parseRequest.ts`)는 세로 병합 이어짐 행을 새 그룹으로 오인하지 않아야
  하며, 가격 칸의 `?`/`-` 등 비숫자는 0이 아닌 null(가격 미정)로 처리한다

## 가격 규칙 (실수 이력에서 배운 것 — 어기지 말 것)

1. **정상가의 유일한 진실원천은 최신 수가표 PDF**(원내 배포, 예: v77)다.
   과거 요청서(마스터 파일)의 이벤트가·런칭가를 정상가로 오인하지 말 것.
   (사례: 쿨소닉 런칭가 119.9만을 정상가로 잘못 사용 → 실제 수가표 정상가는 65만)
   타원 포스터의 "정상가"도 앵커링용이라(에디트의원은 우리보다 5~35% 높게 표기) 우리 정상가로 옮겨 적지 말 것.
2. **n회권 정상가 = 1회 정상가 × n**으로 반드시 검산. 단, **원내에 n회 패키지 정가가 따로 있으면 그것이 우선**이다.
   (사례: 콜라겐 리프팅케어 3회 정상가 25만으로 잘못 기재 → 수가표 1회 6만 × 3 = 18만이 정답 ·
   반대 사례: LDM 물방울 12분 5회는 1회 6만×5=30만이 아니라 5회 패키지 정가 26만 — 과거 요청서에서 같은 n회권 정가를 먼저 찾아볼 것)
3. D열은 부가세 전, E열 = D × 1.1. **EMR 티켓가(부가세 포함)가 기준인 이벤트**(예: 레티젠 아이)는
   D를 만원 단위로 반올림하되 **이벤트가는 올림·정상가는 내림**(고객이 표시가보다 더 내거나
   할인율이 부풀려지는 방향 금지), E열에는 EMR 실결제액을 그대로 둔다.
4. 연장 이벤트는 전월과 **가격 동결** + 비고에 `26년 N월 이벤트 연장` 표기.

## 이벤트 구성 규칙

- **같은 시술을 두 그룹에 중복 배치 금지.** 플친 한정가로 옮기면 원래 월간 그룹에서는 뺀다.
  (사례: 물빛토닝이 월간 9만·플친 8만으로 동시 등장 → 플친만 남기고 제거됨)
- **런칭 이벤트에는 관련 옵션가 라인을 병기**한다. (사례: 힐로웨이브id 런칭에
  `옵션가) 힐로웨이브 2cc`, `옵션가) 소프웨이브 50펄스` 병기 · 쿨소닉/울쎄라피 런칭 관례와 동일)
- 시술 조건은 시술명에 직접 표기: 시술 간격(`(4-6주 간격)`), 별도 내원(`*스킨보톡스는 다른 날
  시술 가능`), 선택형(`(A/B 中 택1)`), 요일 한정(`[화수목]`), 압출 여부(`(압출 미포함)`),
  팁 선택(`(라인팁 / 펜타입 中 택 · 동일가)`) 등 — 고객이 포스터만 보고 조건을 알 수 있어야 한다.
- 플친 3종은 **2~3개월 단위로 유지 후 전면 교체** (대표 결정 · 과거 관례도 "2개월 정도" 표기).
  라인업은 동결하되 **발송 카피/묶음 각도는 매회 새로** 구성해 소재 중복 지적을 피한다.
- **그룹 안 시술은 이벤트가 오름차순으로 나열**하고 `옵션가)` 줄은 맨 아래에 둔다 (원내 검수 요청 ·
  생성기의 `sortItemsByPrice: true`가 자동 적용). 계획 JSON도 같은 순서로 적어 둔다.
- 리투오 등 "리프팅 병행 옵션가"가 있는 시술을 단품 이벤트로 낼 때는 옵션가와의 가격 관계를
  비고에 명시해 원내 혼선을 막는다.

## 의료광고 표현 주의

- 연예인 실명(예: "하지원 리프팅") 금지 · 타 기기명 오인 표기(쿨소닉을 "울쎄라"로 지칭) 금지
- 최상급·과장("핵폭탄", "끝판") 순화 · "무통/안 아픈"은 심의 시 "통증 부담을 줄인"으로
- 증정품은 시술 연관 저가 사은품 수준 유지(고가 경품은 환자유인 소지)

## 이벤트 반응 기록

- 기획 탭 **📚 과거 참고**에서 이벤트(그룹)별 **상/중/하 + 메모**를 입력할 수 있다
  (KV `bp:feedback`에 저장, `/api/auth?action=feedback-get|feedback-set`).
- 다음 달 기획 시 이 기록을 반드시 반영: **반응 '상' 계열은 유지·변형, '하'는 제외**.
  AI 패키지 추천 프롬프트에도 자동 포함된다.

## 이벤트 히스토리 탭

- 대시보드 **📚 이벤트 히스토리**(`#history`)에서 전체 월을 브라우징하고 시술명으로 가격 흐름을 검색한다.
  반응(상/중/하·메모) 입력도 여기서 가능 — `FeedbackChips` 공용 컴포넌트.
- **25.10~26.4 요청서는 열 순서가 달라** 히스토리에 정상가<이벤트가로 뒤집혀 들어와 있다.
  `history.ts`가 로드 시 큰 값을 정상가로 자동 교정하고 `fixedMonths`로 표시한다.
  정상가가 비어 있던 줄은 이벤트가 칸 값이 실제로는 정상가일 수 있으니 그 달 단가는 참고만 할 것.

## 데이터 관리

- 월별 **기획 초안**은 `tools/plans/YYYY-MM.json`에 남기고, 요청서 엑셀은
  `python3 tools/build_request_xlsx.py tools/plans/2026-10.json "10월 디자인 작업 요청서.xlsx"`로 생성한다
  (원내 서식 그대로 — 병합 타이틀·C/D/E 가격열·비고). 기획 배경·검토 항목은
  `docs/events/YYYY-MM-기획메모.md`에 기록한다.
- 월별 **확정본**은 `dashboard/src/data/history.json`에 `"YYYY.M"` 키로 추가한다
  (기획 탭이 참고하는 데이터). 형식: `{title, emphasis, groups:[{group, items:[{name, event, normal}]}]}`
- 원내 **패키지 카탈로그**(부가세 포함가)와 분석·개편안(v2)은 `docs/packages/`에 둔다 — n회권·프로그램 정가는 여기서 먼저 찾는다
- **인스타·대행사 협업 문서**(기획안 회신·브랜딩 결정)는 `docs/instagram/`에 둔다. 대외 회신은 병원 기본정보(`docs/01`)·검수 루프(`docs/07`)·브랜드 가이드(`docs/10`)와 어긋나지 않게 쓴다
- **경쟁원 조사**(이벤트 포스터 수치·가격 비교·대응안)는 `docs/competitors/`에 둔다. 타원 이미지 원본은 repo에 넣지 않고 수치와 출처 URL만 기록한다
- 확정본 원본(엑셀)은 원내 마스터 파일(`2026_master.xlsx`)에서 관리되므로 repo에는 넣지 않는다
- 업로드된 첨부(수가표 PDF·마스터 엑셀)는 세션이 끝나면 사라지므로, **재사용할 수치는 반드시
  repo(plans/메모/history)에 적어둔다.**
